# Wander V2.0 项目交接文档

Wander 是一个高保真、多主题的 Chrome New Tab 插件。它通过 Chrome Bookmarks API 随机抽取书签，并以精致的杂志排版（Editorial Collage）形式呈现，旨在让用户在每日的碎片时间里重新发现那些被遗忘的收藏链接。

## 核心设计理念

- **Resurface**: 将沉睡在书签栏深处的链接带回视野。
- **Editorial**: 模仿纸质媒体的版面设计，强调比例、节奏和留白。
- **Variety**: 提供 26 套截然不同的视觉主题，涵盖从古典报刊到极简主义，再到复古 Windows 等多种风格。

---

## 文件结构与职能

项目采用无构建流程的纯原生开发，便于直接加载和快速迭代。

```text
Wander-newtab-v2/
├── manifest.json         # 插件配置文件 (V3)，声明权限与入口
├── newtab.html           # 新标签页主结构与全量 CSS 样式（含 11 种卡片布局）
├── newtab.js             # 核心逻辑：书签抽取、布局匹配、动效编排
├── themes.js             # 静态数据：定义 26 套主题的颜色、字体、动效类型
├── popup.html / .js      # 插件工具栏弹窗：主题切换、随机模式控制
├── layout-playground.html # 开发辅助：布局系统压力测试与预览
├── transition-playground.html # 开发辅助：动画效果调试工具
└── icons/                # 插件图标资源
```

---

## 核心系统架构

### 1. 主题系统 (Theme Engine)
主题定义在 `themes.js` 中，通过 `newtab.js` 的 `applyTheme()` 函数动态注入。
- **CSS 变量驱动**: 主题通过改变 `--canvas`, `--canvas-ink`, `--paper` 等变量实现一键变色。
- **动态字体**: 支持通过 Google Fonts 动态加载远程字体。
- **环境光效**: `ambientGlow` 效果会根据当前主题的 Palette 自动计算渐变色。
- **霓虹效果**: 部分暗色/混合主题支持 `neonAccents`，增强视觉冲击力。

### 2. 布局系统 (Layout Engine)
Wander 使用了一套严谨的几何布局算法，确保 6 张不同比例的卡片总能组合成一个边缘齐平的完美长方形。
- **12 套栅格 (`LAYOUTS`)**: 在 `newtab.js` 中定义了 a 到 l 共 12 种组合方案。
- **11 种卡片组件 (`l-layouts`)**: `newtab.html` 中通过 CSS 类（如 `l-magazine`, `l-bento`, `l-brutalist`）定义了内部信息排布。
- **响应式**: 适配桌面端宽屏展示，并针对移动端宽度（如在测试页中）有基础兼容。

### 3. 动画与交互 (Animation System)
Shuffle 过程并非简单的刷新，而是一个多阶段的物理动效。
- **转换类型**: 每个主题绑定一种动效类型（`slice`, `blind`, `iris`, `flip`, `elevator`, `zoomout`, `hinge`, `split`）。
- **洗牌流程**: 
    1. **Cut**: 原有卡片缩放、模糊并移出。
    2. **Deal**: 新卡片带有随机的 `tilt`（倾斜）和 `drift`（偏移）进入。
    3. **Settle**: 经过约 800ms，卡片自动对齐，恢复完美几何状态。

---

## 开发与维护指南

### 如何添加新主题
1. 在 `themes.js` 的 `THEMES` 对象中新增条目。
2. 定义 `preview` 颜色数组（用于 Popup 显示）。
3. 选择一个 `layout`（组件样式）和 `type`（动效类型）。
4. 在 `THEME_ORDER` 数组中添加对应 Key。

### 如何修改布局
- **栅格调整**: 修改 `newtab.js` 中的 `LAYOUTS` 数组。`sizes` 定义了卡片的比例等级（hero, wide, tall, small, strip）。
- **视觉风格**: 在 `newtab.html` 的 `<style>` 标签中搜索对应的 `.bookmark.l-[name]` 类进行修改。

### 验证点
- **对比度检查**: `isColorDark()` 函数会自动检测卡片背景色，并切换 `is-dark-card` 类以保证文字可读性。
- **性能**: 所有的动效均使用 `transform` 和 `opacity`，确保在低配设备上也能达到 60fps。

---

## 部署与分发

### 本地测试
1. 在 Chrome 地址栏输入 `chrome://extensions/`。
2. 开启 “开发者模式”。
3. 点击 “加载已解压的扩展程序”，选择 `Wander-newtab-v2` 文件夹。

### 打包发布
1. **清理**: 删除 `layout-playground.html` 和 `transition-playground.html`（可选，不影响功能但能减小体积）。
2. **压缩**: 将所有核心文件压缩为 `wander-v2.zip`。
3. **版本管理**: 更新 `manifest.json` 中的 `version` 字段。

---

## 未来进阶方向 (Codex / AI 助手参考)

1. **书签筛选**: 目前是全量随机，可以增加“只随机收藏夹 A”的功能。
2. **视觉抓取**: 引入低优先级的 API 抓取网站的 Meta Image 或 Description，进一步丰富排版。
3. **交互深度**: 长按卡片显示书签详情或快速重命名。
4. **离线模式**: 目前字体依赖 Google CDN，可以考虑将核心字体本地化以实现完全离线可用。

---
*Generated by Antigravity - 2026.05.14*
